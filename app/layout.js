import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Aequitas Law Partners - Premium Legal Services",
  description: "Aequitas Law Partners provides comprehensive legal services with integrity, expertise, and dedication. Serving clients with professional excellence in various practice areas.",
  keywords: "law firm, legal services, attorney, lawyer, legal consultation, Aequitas Law Partners",
  authors: [{ name: "Aequitas Law Partners" }],
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
